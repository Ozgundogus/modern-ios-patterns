import Foundation

extension URL {
    static let avatar = URL(string: "https://example.com/avatar.png")!
    static let banner = URL(string: "https://example.com/banner.png")!
}

extension Data {
    static let image = Data([0x89, 0x50, 0x4E, 0x47])
}
